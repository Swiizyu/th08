# SYNC notes — Agent 2 "Санаэ" (arena/01a01ccf-th08)

- Zone: src/Gui.*, src/MusicRoom.*, src/ResultScreen.*, src/TitleScreen.*, src/Ending.*, src/GameManager.*, src/AsciiManager.*
- CI: workflow_dispatch is 403 for the bot token, so builds on this branch are
  triggered via a PR to main (CI vehicle only — integration still goes through
  a PR into arena/01a01c02-th08 per AGENTS.md rule 5).
